# Arpass YubiKey-only vault recovery — Python dependencies
#
# python-fido2: CTAP2 / WebAuthn PRF (hmac-secret) over USB HID.
#   Pinned to the 1.x API used by recover-yubikey.py. The tool also handles the
#   2.x Fido2Client(client_data_collector=...) constructor, so >=1.1.3 is the
#   floor; 2.x works too. If you install 2.x and hit an API error, pin <2.0.0.
fido2>=1.1.3

# cryptography: HKDF-SHA256 + AES-256-GCM.
cryptography>=41.0.0
